services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: clarify
      POSTGRES_PASSWORD: clarify_dev
      POSTGRES_DB: clarify
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: